# ==============================================================================
# Detección de Placas Vehiculares — Popayán
# requirements_colab.txt — dependencias para Google Colab / Jupyter
#
# Uso dentro de una celda Colab:
#   !pip install -r requirements_colab.txt -q
#
# O celdas separadas (recomendado para evitar conflictos de versiones):
#   !pip install ultralytics huggingface_hub easyocr -q
#   !apt-get install tesseract-ocr libtesseract-dev -y -q
#   !pip install pytesseract pandas pillow-avif-plugin -q
#   !pip install gradio -q
#
# NOTA: torch, torchvision, numpy y scikit-learn ya vienen preinstalados
#       en el entorno Colab estándar. Solo se reinstalan si se necesita
#       una versión específica.
# ==============================================================================

# ── Detección de objetos ─────────────────────────────────────────────────────
ultralytics>=8.0.0
huggingface_hub>=0.20.0

# ── Visión computacional ─────────────────────────────────────────────────────
opencv-python>=4.8.0
Pillow>=9.0.0
pillow-avif-plugin

# ── OCR ──────────────────────────────────────────────────────────────────────
easyocr>=1.7.0
pytesseract>=0.3.10
# Tesseract (sistema): !apt-get install tesseract-ocr libtesseract-dev -y -q

# ── Interfaz web ─────────────────────────────────────────────────────────────
gradio>=4.0.0

# ── Datos (complemento — Colab ya incluye numpy, pandas, sklearn base) ───────
pandas>=1.5.0
scikit-learn>=1.0.0
seaborn>=0.12.0